server {
    listen 80;
    server_name localhost;

    location / {
        default_type text/plain;
        return 200 "Backend 2\n";
    }

    location /health {
        default_type text/plain;
        return 200 "OK";
    }
} 